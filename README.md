# João Victor — Portfolio

Landing page estática (HTML + CSS) do portfólio do João Victor, videomaker.

- `index.html` — página única
- `styles.css` — estilos
- `hero.jpg` — imagem do hero

Deploy: Vercel (static). Domínio padrão gerado pelo Vercel.
